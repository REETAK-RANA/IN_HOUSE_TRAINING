1. Write a Python function that takes a list of numbers as input and returns the sum of all even numbers in the list.

def main(number):
    return sum(num for num in number if num % 2==0)
number=[1,2,3,4,5]
print(main(number))

2. Create a Python function that accepts a string and returns the reverse of that string.

def main(delta):
 return delta[::-1]
delta="reetak , rana"
print(main(delta))

3. Implement a Python function that takes a list of integers and returns a new list containing the squares of each number.

def main(delta):
    return [num ** 2 for num in delta]
delta=[1,2,3,4,5]
print(main(delta))

4. Create an iterator class in Python that generates the Fibonacci sequence up to a specified number of terms.

a = 0
b = 1
n = int(input("How many terms? "))

for i in range(n):
    print(a)
    a, b = b, a + b

5. Write a generator function in Python that yields the powers of 2 up to a given exponent.

def main(n):
    for i in range(n+1):
        yield 2**1
for power in main(5):
    print(power)

6. Use a lambda function in Python to sort a list of tuples based on the second element of each tuple.

x=[(1,2),(3,4)]
x.sort(key=lambda x: x[1])
print(x)

7. Write a Python program that uses map() to convert a list of temperatures from Celsius to Fahrenheit.

celsius=[0,20,30,37]
fahrenheit=list(map(lambda x:(x*9/5)+ 32 ,celsius))
print("celsius",celsius)
print("fahrenheit",fahrenheit)

8. Create a Python program that uses 'filter()` to remove all the vowels from a given string.
def main(delta):
    vowels = 'aeiouAEIOU'
    return ''.join(filter(lambda x:x not in vowels , s))
s=str(input('enter any alphabet'))
print(main(delta))

9. create a function that returns a list of squares in python program .

def main():
  return[i**2 for i in range(1,n+1)]
print(main(5))

